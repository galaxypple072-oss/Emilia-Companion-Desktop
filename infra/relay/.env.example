# Public DNS name whose A/AAAA record points at this server.
RELAY_DOMAIN=relay.example.com

# Generate with: printf '/'; openssl rand -hex 24
# Keep the leading slash. Treat this unguessable path as deployment metadata.
RELAY_PATH=/replace-with-48-random-hex-characters
