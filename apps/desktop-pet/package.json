{
  "name": "@personal-companion/desktop-pet",
  "version": "0.0.5",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "test": "node --test test/*.test.js",
    "bundle:mac": "cd src-tauri && ../node_modules/.bin/tauri build --ci",
    "bundle:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File ../../scripts/prepare-windows-core-runtime.ps1 && cd src-tauri && ..\\node_modules\\.bin\\tauri.cmd build --ci",
    "tauri": "tauri"
  },
  "dependencies": {
    "live2d-renderer": "0.6.6",
    "path-browserify": "1.0.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "vite": "8.2.2"
  }
}
